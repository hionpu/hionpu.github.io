```c#
        private void IntuitiveProcessTargetNodes()
        {
            foreach (HmSceneNode node in TargetNodes)
            {
                if (_oldRefPoint == null) _oldRefPoint = _refPoint;
                var tempMatrix = operation(_refPoint, _oldRefPoint, node.ModelMatrix);
                node.ModelMatrix = tempMatrix;
            }
        }
```